to run the code
----------------

1)clone the repository 
2)get java jre
3)open the folder where there is the file pom.xml in the command shell
4)run the code for windows typing: mvnw.cmd clean javafx:run
	       for linux/macos   : ./mvnw clean javafx:run

